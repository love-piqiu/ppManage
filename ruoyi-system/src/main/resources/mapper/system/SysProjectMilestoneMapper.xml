<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ruoyi.system.mapper.SysProjectMilestoneMapper">

    <resultMap type="SysProjectMilestone" id="SysProjectMilestoneResult">
        <id     property="id"              column="id"               />
        <result property="projectId"       column="project_id"       />
        <result property="projectName"     column="project_name"     />
        <result property="description"     column="description"      />
        <result property="recordDate"      column="record_date"      />
        <result property="delFlag"         column="del_flag"         />
        <result property="createBy"        column="create_by"        />
        <result property="createTime"      column="create_time"      />
    </resultMap>

    <sql id="selectMilestoneVo">
        select m.id, m.project_id, p.name as project_name, m.description, m.record_date,
               m.del_flag, m.create_by, m.create_time
        from sys_project_milestone m
        left join sys_project p on m.project_id = p.id
    </sql>

    <select id="selectByProjectId" parameterType="Long" resultMap="SysProjectMilestoneResult">
        <include refid="selectMilestoneVo"/>
        where m.project_id = #{projectId} and m.del_flag = '0'
        order by m.record_date desc
    </select>

    <select id="selectById" parameterType="Long" resultMap="SysProjectMilestoneResult">
        <include refid="selectMilestoneVo"/>
        where m.id = #{id} and m.del_flag = '0'
    </select>

    <insert id="insert" parameterType="SysProjectMilestone" useGeneratedKeys="true" keyProperty="id">
        insert into sys_project_milestone(
            <if test="projectId != null">project_id,</if>
            <if test="description != null and description != ''">description,</if>
            <if test="recordDate != null">record_date,</if>
            <if test="createBy != null and createBy != ''">create_by,</if>
            create_time
        )values(
            <if test="projectId != null">#{projectId},</if>
            <if test="description != null and description != ''">#{description},</if>
            <if test="recordDate != null">#{recordDate},</if>
            <if test="createBy != null and createBy != ''">#{createBy},</if>
            sysdate()
        )
    </insert>

    <delete id="deleteById" parameterType="Long">
        update sys_project_milestone set del_flag = '2' where id = #{id}
    </delete>

    <delete id="deleteByProjectId" parameterType="Long">
        update sys_project_milestone set del_flag = '2' where project_id = #{projectId}
    </delete>

</mapper>